apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagster.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dagster.labels" . | nindent 4 }}
type: Opaque
stringData:
  db-password: "changeme-replace-in-production"

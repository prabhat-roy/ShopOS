apiVersion: v1
kind: Service
metadata:
  name: {{ include "netdata.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "netdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netdata.selectorLabels" . | nindent 4 }}

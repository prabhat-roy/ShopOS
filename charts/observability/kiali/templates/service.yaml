apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiali.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kiali.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiali.selectorLabels" . | nindent 4 }}

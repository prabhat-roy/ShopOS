# MongoDB connection URI (required)
MONGODB_URI=mongodb://localhost:27017

# MongoDB database name (default: catalog)
DB_NAME=catalog

# HTTP server port (default: 8110)
HTTP_PORT=8110

# gRPC server port (default: 50070)
GRPC_PORT=50070

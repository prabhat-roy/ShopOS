# price-list-service environment variables
# Copy to .env and fill in real values before running locally.

HTTP_PORT=8114

# PostgreSQL connection
DATABASE_URL=jdbc:postgresql://localhost:5432/pricelist
DB_USER=pricelist
DB_PASSWORD=changeme

# Optional: gRPC port (reserved for future gRPC implementation)
GRPC_PORT=50181

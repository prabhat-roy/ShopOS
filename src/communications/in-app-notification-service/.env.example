# Redis connection URL
REDIS_URL=redis://localhost:6379

# HTTP server port
HTTP_PORT=8505

# gRPC server port
GRPC_PORT=50132

# Notification TTL in days (default: 30)
NOTIF_TTL_DAYS=30

# Maximum notifications stored per user (oldest are trimmed)
MAX_PER_USER=100

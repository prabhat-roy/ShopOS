# HTTP server port
HTTP_PORT=8193

# gRPC server port
GRPC_PORT=50189

# Redis connection URL (conversation state)
REDIS_URL=redis://localhost:6379/0

# Session TTL in seconds (default 30 minutes)
SESSION_TTL_SECONDS=1800

# Logging verbosity
LOG_LEVEL=info

# geolocation-service environment variables
# Copy to .env and adjust values for local development.

# HTTP server port (default: 8093)
HTTP_PORT=8093

# gRPC server port (default: 50058)
GRPC_PORT=50058

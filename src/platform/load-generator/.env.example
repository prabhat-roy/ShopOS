# Load Generator — environment variable reference
# Copy to .env and adjust values before running locally.

HTTP_PORT=8089
TARGET_BASE_URL=http://localhost:8080
DEFAULT_RPS=10.0
RAMP_UP_SECONDS=30
SCENARIO=browse
DURATION_SECONDS=300
CONCURRENCY=10

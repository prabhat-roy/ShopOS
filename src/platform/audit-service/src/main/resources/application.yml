# All configuration is intentionally kept in application.properties.
# This file is retained as a placeholder; add profile-specific overrides here
# if needed (e.g. spring.config.activate.on-profile: docker).

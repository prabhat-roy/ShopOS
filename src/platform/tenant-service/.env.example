# HTTP server port (default: 8087)
HTTP_PORT=8087

# PostgreSQL connection string
DATABASE_URL=postgres://postgres:password@localhost:5432/tenants?sslmode=disable

# Maximum number of open database connections (default: 10)
DB_MAX_CONNS=10

# Database connection timeout (default: 5s)
DB_TIMEOUT=5s

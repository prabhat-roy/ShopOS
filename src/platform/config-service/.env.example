HTTP_PORT=8090
GRPC_PORT=50051
ENV=development

# Etcd cluster endpoints (comma-separated)
ETCD_ADDRS=localhost:2379
ETCD_TIMEOUT=5s

# Key prefix for all config entries in etcd
CONFIG_PREFIX=/shopos/config

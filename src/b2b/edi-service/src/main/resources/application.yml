spring:
  application:
    name: edi-service

server:
  port: 50165

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  endpoint:
    health:
      show-details: always

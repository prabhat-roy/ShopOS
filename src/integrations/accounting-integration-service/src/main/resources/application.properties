server.port=${SERVER_PORT:8900}
spring.application.name=accounting-integration-service

# Actuator
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# Logging
logging.level.com.shopos=${LOGGING_LEVEL_COM_SHOPOS:INFO}

# HTTP server port (default 8906)
HTTP_PORT=8906

# gRPC server port (default 50175)
GRPC_PORT=50175

# Default tax provider when none is specified in the request: AVALARA | TAXJAR | VERTEX | INTERNAL
DEFAULT_PROVIDER=INTERNAL

# Log verbosity: debug | info | warn | error
LOG_LEVEL=info

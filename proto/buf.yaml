version: v2
modules:
  - path: .
    name: buf.build/shopos/api
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc/grpc
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_VERSION_SUFFIX
  ignore:
    - common/
breaking:
  use:
    - FILE

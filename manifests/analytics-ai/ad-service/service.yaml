apiVersion: v1
kind: Service
metadata:
  name: ad-service
  namespace: analytics-ai
  labels:
    app: ad-service
    domain: analytics-ai
spec:
  selector:
    app: ad-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50154
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: search-analytics-service
  namespace: analytics-ai
  labels:
    app: search-analytics-service
    domain: analytics-ai
spec:
  selector:
    app: search-analytics-service
  type: ClusterIP
  ports:
    - name: http
      port: 8196
      targetPort: http
      protocol: TCP

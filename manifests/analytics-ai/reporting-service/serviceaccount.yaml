apiVersion: v1
kind: ServiceAccount
metadata:
  name: reporting-service
  namespace: analytics-ai
  labels:
    app: reporting-service
    domain: analytics-ai
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-reporting-service

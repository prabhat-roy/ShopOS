apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: analytics-ai
  labels:
    app: reporting-service
    domain: analytics-ai
spec:
  selector:
    app: reporting-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-in-stock-service
  namespace: customer-experience
  labels:
    app: back-in-stock-service
    domain: customer-experience
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: back-in-stock-service
  template:
    metadata:
      labels:
        app: back-in-stock-service
        domain: customer-experience
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50187"
    spec:
      serviceAccountName: back-in-stock-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: back-in-stock-service
          image: shopos/back-in-stock-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50187
              protocol: TCP
          envFrom:
            - configMapRef:
                name: back-in-stock-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50187
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50187
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: back-in-stock-service

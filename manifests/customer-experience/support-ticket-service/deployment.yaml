apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-ticket-service
  namespace: customer-experience
  labels:
    app: support-ticket-service
    domain: customer-experience
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: support-ticket-service
  template:
    metadata:
      labels:
        app: support-ticket-service
        domain: customer-experience
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50125"
    spec:
      serviceAccountName: support-ticket-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: support-ticket-service
          image: shopos/support-ticket-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50125
              protocol: TCP
          envFrom:
            - configMapRef:
                name: support-ticket-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50125
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50125
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: support-ticket-service

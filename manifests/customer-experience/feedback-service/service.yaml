apiVersion: v1
kind: Service
metadata:
  name: feedback-service
  namespace: customer-experience
  labels:
    app: feedback-service
    domain: customer-experience
spec:
  selector:
    app: feedback-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50182
      targetPort: grpc
      protocol: TCP

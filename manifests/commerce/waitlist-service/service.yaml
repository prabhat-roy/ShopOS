apiVersion: v1
kind: Service
metadata:
  name: waitlist-service
  namespace: commerce
  labels:
    app: waitlist-service
    domain: commerce
spec:
  selector:
    app: waitlist-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50183
      targetPort: grpc
      protocol: TCP

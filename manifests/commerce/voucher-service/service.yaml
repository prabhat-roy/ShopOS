apiVersion: v1
kind: Service
metadata:
  name: voucher-service
  namespace: commerce
  labels:
    app: voucher-service
    domain: commerce
spec:
  selector:
    app: voucher-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50097
      targetPort: grpc
      protocol: TCP

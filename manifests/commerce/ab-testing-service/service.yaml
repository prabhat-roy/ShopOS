apiVersion: v1
kind: Service
metadata:
  name: ab-testing-service
  namespace: commerce
  labels:
    app: ab-testing-service
    domain: commerce
spec:
  selector:
    app: ab-testing-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50093
      targetPort: grpc
      protocol: TCP

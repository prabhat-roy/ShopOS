apiVersion: v1
kind: Service
metadata:
  name: loyalty-service
  namespace: commerce
  labels:
    app: loyalty-service
    domain: commerce
spec:
  selector:
    app: loyalty-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50088
      targetPort: grpc
      protocol: TCP

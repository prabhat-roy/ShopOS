apiVersion: v1
kind: Service
metadata:
  name: address-validation-service
  namespace: commerce
  labels:
    app: address-validation-service
    domain: commerce
spec:
  selector:
    app: address-validation-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50095
      targetPort: grpc
      protocol: TCP

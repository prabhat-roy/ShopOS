apiVersion: v1
kind: ServiceAccount
metadata:
  name: address-validation-service
  namespace: commerce
  labels:
    app: address-validation-service
    domain: commerce
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-address-validation-service

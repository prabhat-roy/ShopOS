apiVersion: v1
kind: Service
metadata:
  name: subscription-billing-service
  namespace: commerce
  labels:
    app: subscription-billing-service
    domain: commerce
spec:
  selector:
    app: subscription-billing-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50090
      targetPort: grpc
      protocol: TCP

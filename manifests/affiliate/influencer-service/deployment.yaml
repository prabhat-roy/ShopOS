apiVersion: apps/v1
kind: Deployment
metadata:
  name: influencer-service
  namespace: affiliate
  labels:
    app: influencer-service
    domain: affiliate
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: influencer-service
  template:
    metadata:
      labels:
        app: influencer-service
        domain: affiliate
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50202"
    spec:
      serviceAccountName: influencer-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: influencer-service
          image: shopos/influencer-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50202
              protocol: TCP
          envFrom:
            - configMapRef:
                name: influencer-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50202
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50202
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: influencer-service

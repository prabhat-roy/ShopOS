apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-export-service
  namespace: content
  labels:
    app: data-export-service
    domain: content
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-export-service
  template:
    metadata:
      labels:
        app: data-export-service
        domain: content
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50147"
    spec:
      serviceAccountName: data-export-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: data-export-service
          image: shopos/data-export-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50147
              protocol: TCP
          envFrom:
            - configMapRef:
                name: data-export-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50147
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50147
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: data-export-service

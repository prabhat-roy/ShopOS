apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: content
  labels:
    app: document-service
    domain: content
spec:
  selector:
    app: document-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50142
      targetPort: grpc
      protocol: TCP

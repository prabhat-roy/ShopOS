apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-key-service
  namespace: identity
  labels:
    app: api-key-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-api-key-service

apiVersion: v1
kind: Service
metadata:
  name: gdpr-service
  namespace: identity
  labels:
    app: gdpr-service
    domain: identity
spec:
  selector:
    app: gdpr-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50065
      targetPort: grpc
      protocol: TCP

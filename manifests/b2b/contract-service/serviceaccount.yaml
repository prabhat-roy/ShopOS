apiVersion: v1
kind: ServiceAccount
metadata:
  name: contract-service
  namespace: b2b
  labels:
    app: contract-service
    domain: b2b
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-contract-service

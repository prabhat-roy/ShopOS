apiVersion: v1
kind: Service
metadata:
  name: quote-rfq-service
  namespace: b2b
  labels:
    app: quote-rfq-service
    domain: b2b
spec:
  selector:
    app: quote-rfq-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50162
      targetPort: grpc
      protocol: TCP

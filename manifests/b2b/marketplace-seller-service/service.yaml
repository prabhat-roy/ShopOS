apiVersion: v1
kind: Service
metadata:
  name: marketplace-seller-service
  namespace: b2b
  labels:
    app: marketplace-seller-service
    domain: b2b
spec:
  selector:
    app: marketplace-seller-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50166
      targetPort: grpc
      protocol: TCP

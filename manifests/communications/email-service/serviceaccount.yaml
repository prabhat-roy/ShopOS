apiVersion: v1
kind: ServiceAccount
metadata:
  name: email-service
  namespace: communications
  labels:
    app: email-service
    domain: communications
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-email-service

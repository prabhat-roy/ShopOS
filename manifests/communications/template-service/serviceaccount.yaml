apiVersion: v1
kind: ServiceAccount
metadata:
  name: template-service
  namespace: communications
  labels:
    app: template-service
    domain: communications
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-template-service

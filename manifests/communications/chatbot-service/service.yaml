apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: communications
  labels:
    app: chatbot-service
    domain: communications
spec:
  selector:
    app: chatbot-service
  type: ClusterIP
  ports:
    - name: http
      port: 8193
      targetPort: http
      protocol: TCP

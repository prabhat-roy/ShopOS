apiVersion: v1
kind: Service
metadata:
  name: digest-service
  namespace: communications
  labels:
    app: digest-service
    domain: communications
spec:
  selector:
    app: digest-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

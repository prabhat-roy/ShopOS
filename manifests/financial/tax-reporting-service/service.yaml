apiVersion: v1
kind: Service
metadata:
  name: tax-reporting-service
  namespace: financial
  labels:
    app: tax-reporting-service
    domain: financial
spec:
  selector:
    app: tax-reporting-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50113
      targetPort: grpc
      protocol: TCP

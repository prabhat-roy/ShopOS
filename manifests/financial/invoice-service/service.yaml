apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: financial
  labels:
    app: invoice-service
    domain: financial
spec:
  selector:
    app: invoice-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50110
      targetPort: grpc
      protocol: TCP

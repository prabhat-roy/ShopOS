apiVersion: v1
kind: ServiceAccount
metadata:
  name: brand-service
  namespace: catalog
  labels:
    app: brand-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-brand-service

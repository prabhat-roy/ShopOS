apiVersion: v1
kind: ServiceAccount
metadata:
  name: search-service
  namespace: catalog
  labels:
    app: search-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-search-service

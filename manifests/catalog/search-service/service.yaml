apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: catalog
  labels:
    app: search-service
    domain: catalog
spec:
  selector:
    app: search-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50078
      targetPort: grpc
      protocol: TCP

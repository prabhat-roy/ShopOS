apiVersion: apps/v1
kind: Deployment
metadata:
  name: price-list-service
  namespace: catalog
  labels:
    app: price-list-service
    domain: catalog
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: price-list-service
  template:
    metadata:
      labels:
        app: price-list-service
        domain: catalog
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50181"
    spec:
      serviceAccountName: price-list-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: price-list-service
          image: shopos/price-list-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50181
              protocol: TCP
          envFrom:
            - configMapRef:
                name: price-list-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50181
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50181
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: price-list-service

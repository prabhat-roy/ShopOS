apiVersion: v1
kind: ServiceAccount
metadata:
  name: product-catalog-service
  namespace: catalog
  labels:
    app: product-catalog-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-product-catalog-service

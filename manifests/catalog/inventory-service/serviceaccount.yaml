apiVersion: v1
kind: ServiceAccount
metadata:
  name: inventory-service
  namespace: catalog
  labels:
    app: inventory-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-inventory-service

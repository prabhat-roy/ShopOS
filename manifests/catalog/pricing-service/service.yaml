apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: catalog
  labels:
    app: pricing-service
    domain: catalog
spec:
  selector:
    app: pricing-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50073
      targetPort: grpc
      protocol: TCP

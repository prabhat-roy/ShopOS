apiVersion: v1
kind: ServiceAccount
metadata:
  name: configurator-service
  namespace: catalog
  labels:
    app: configurator-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-configurator-service

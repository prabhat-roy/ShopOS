apiVersion: v1
kind: ServiceAccount
metadata:
  name: subscription-product-service
  namespace: catalog
  labels:
    app: subscription-product-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-subscription-product-service

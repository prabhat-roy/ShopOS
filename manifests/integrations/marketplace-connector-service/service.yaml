apiVersion: v1
kind: Service
metadata:
  name: marketplace-connector-service
  namespace: integrations
  labels:
    app: marketplace-connector-service
    domain: integrations
spec:
  selector:
    app: marketplace-connector-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

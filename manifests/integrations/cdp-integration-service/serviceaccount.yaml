apiVersion: v1
kind: ServiceAccount
metadata:
  name: cdp-integration-service
  namespace: integrations
  labels:
    app: cdp-integration-service
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-cdp-integration-service

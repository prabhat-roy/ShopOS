apiVersion: v1
kind: ServiceAccount
metadata:
  name: crm-integration-service
  namespace: integrations
  labels:
    app: crm-integration-service
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-crm-integration-service

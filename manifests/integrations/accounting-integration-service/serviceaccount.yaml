apiVersion: v1
kind: ServiceAccount
metadata:
  name: accounting-integration-service
  namespace: integrations
  labels:
    app: accounting-integration-service
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-accounting-integration-service

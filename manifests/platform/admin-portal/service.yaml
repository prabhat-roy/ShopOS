apiVersion: v1
kind: Service
metadata:
  name: admin-portal
  namespace: platform
  labels:
    app: admin-portal
    domain: platform
spec:
  selector:
    app: admin-portal
  type: ClusterIP
  ports:
    - name: http
      port: 8085
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  namespace: platform
  labels:
    app: api-gateway
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-api-gateway

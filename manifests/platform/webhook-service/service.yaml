apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: platform
  labels:
    app: webhook-service
    domain: platform
spec:
  selector:
    app: webhook-service
  type: ClusterIP
  ports:
    - name: http
      port: 8091
      targetPort: http
      protocol: TCP

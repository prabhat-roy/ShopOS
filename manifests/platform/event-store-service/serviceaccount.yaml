apiVersion: v1
kind: ServiceAccount
metadata:
  name: event-store-service
  namespace: platform
  labels:
    app: event-store-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-event-store-service

apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator
  namespace: platform
  labels:
    app: saga-orchestrator
    domain: platform
spec:
  selector:
    app: saga-orchestrator
  type: ClusterIP
  ports:
    - name: grpc
      port: 50054
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: platform
  labels:
    app: scheduler-service
    domain: platform
spec:
  selector:
    app: scheduler-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50056
      targetPort: grpc
      protocol: TCP

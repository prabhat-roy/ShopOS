apiVersion: v1
kind: Service
metadata:
  name: health-check-service
  namespace: platform
  labels:
    app: health-check-service
    domain: platform
spec:
  selector:
    app: health-check-service
  type: ClusterIP
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: platform
  labels:
    app: tenant-service
    domain: platform
spec:
  selector:
    app: tenant-service
  type: ClusterIP
  ports:
    - name: http
      port: 8087
      targetPort: http
      protocol: TCP

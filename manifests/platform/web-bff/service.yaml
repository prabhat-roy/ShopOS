apiVersion: v1
kind: Service
metadata:
  name: web-bff
  namespace: platform
  labels:
    app: web-bff
    domain: platform
spec:
  selector:
    app: web-bff
  type: ClusterIP
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP

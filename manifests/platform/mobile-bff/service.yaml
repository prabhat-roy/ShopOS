apiVersion: v1
kind: Service
metadata:
  name: mobile-bff
  namespace: platform
  labels:
    app: mobile-bff
    domain: platform
spec:
  selector:
    app: mobile-bff
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP

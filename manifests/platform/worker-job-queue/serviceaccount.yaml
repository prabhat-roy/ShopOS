apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker-job-queue
  namespace: platform
  labels:
    app: worker-job-queue
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-worker-job-queue

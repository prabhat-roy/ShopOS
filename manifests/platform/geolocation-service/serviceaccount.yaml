apiVersion: v1
kind: ServiceAccount
metadata:
  name: geolocation-service
  namespace: platform
  labels:
    app: geolocation-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-geolocation-service

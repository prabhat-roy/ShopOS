apiVersion: v1
kind: Service
metadata:
  name: geolocation-service
  namespace: platform
  labels:
    app: geolocation-service
    domain: platform
spec:
  selector:
    app: geolocation-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50058
      targetPort: grpc
      protocol: TCP

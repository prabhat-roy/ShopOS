apiVersion: v1
kind: Service
metadata:
  name: returns-logistics-service
  namespace: supply-chain
  labels:
    app: returns-logistics-service
    domain: supply-chain
spec:
  selector:
    app: returns-logistics-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50108
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: customs-duties-service
  namespace: supply-chain
  labels:
    app: customs-duties-service
    domain: supply-chain
spec:
  selector:
    app: customs-duties-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50107
      targetPort: grpc
      protocol: TCP

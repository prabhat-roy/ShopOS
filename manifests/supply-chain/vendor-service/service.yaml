apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: supply-chain
  labels:
    app: vendor-service
    domain: supply-chain
spec:
  selector:
    app: vendor-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50100
      targetPort: grpc
      protocol: TCP

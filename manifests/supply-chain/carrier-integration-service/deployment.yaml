apiVersion: apps/v1
kind: Deployment
metadata:
  name: carrier-integration-service
  namespace: supply-chain
  labels:
    app: carrier-integration-service
    domain: supply-chain
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: carrier-integration-service
  template:
    metadata:
      labels:
        app: carrier-integration-service
        domain: supply-chain
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50106"
    spec:
      serviceAccountName: carrier-integration-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: carrier-integration-service
          image: shopos/carrier-integration-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50106
              protocol: TCP
          envFrom:
            - configMapRef:
                name: carrier-integration-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50106
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50106
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: carrier-integration-service

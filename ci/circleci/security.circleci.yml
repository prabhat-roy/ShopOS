# Security Infrastructure Pipeline — CircleCI
# Context: shopos-kubernetes, shopos-keycloak, shopos-slack
version: 2.1

executors:
  helm-executor:
    docker:
      - image: alpine/helm:latest
    resource_class: medium

jobs:
  install-cert-manager:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install cert-manager
          command: |
            [ "$CERT_MANAGER" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add jetstack https://charts.jetstack.io --force-update
            helm upgrade --install cert-manager jetstack/cert-manager \
              --namespace cert-manager --create-namespace \
              --set installCRDs=true --set replicaCount=2 \
              --wait --timeout 5m

  install-vault:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install Vault HA
          command: |
            [ "$VAULT" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add hashicorp https://helm.releases.hashicorp.com --force-update
            helm upgrade --install vault hashicorp/vault \
              --namespace vault --create-namespace \
              --set server.ha.enabled=true --set server.ha.raft.enabled=true \
              --set server.ha.replicas=3 --wait --timeout 10m

  install-keycloak:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install Keycloak
          command: |
            [ "$KEYCLOAK" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add bitnami https://charts.bitnami.com/bitnami --force-update
            helm upgrade --install keycloak bitnami/keycloak \
              --namespace keycloak --create-namespace \
              --set replicaCount=2 \
              --set auth.adminPassword="$KEYCLOAK_ADMIN_PASSWORD" \
              --set postgresql.enabled=true --wait --timeout 10m

  install-kyverno:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install Kyverno
          command: |
            [ "$KYVERNO" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add kyverno https://kyverno.github.io/kyverno --force-update
            helm upgrade --install kyverno kyverno/kyverno \
              --namespace kyverno --create-namespace \
              --set replicaCount=3 --wait --timeout 5m
            kubectl apply -f security/kyverno/ 2>/dev/null || true

  install-falco:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install Falco
          command: |
            [ "$FALCO" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add falcosecurity https://falcosecurity.github.io/charts --force-update
            helm upgrade --install falco falcosecurity/falco \
              --namespace falco --create-namespace \
              --set driver.kind=ebpf \
              --set falcosidekick.enabled=true \
              --set falcosidekick.config.slack.webhookurl="$SLACK_WEBHOOK" \
              --wait --timeout 8m

  install-opa:
    executor: helm-executor
    steps:
      - checkout
      - run:
          name: Setup kubeconfig
          command: mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - run:
          name: Install OPA Gatekeeper
          command: |
            [ "$OPA" = "true" ] || { echo "Skipped"; exit 0; }
            helm repo add gatekeeper https://open-policy-agent.github.io/gatekeeper/charts --force-update
            helm upgrade --install gatekeeper gatekeeper/gatekeeper \
              --namespace gatekeeper-system --create-namespace \
              --set replicaCount=3 --wait --timeout 5m
            kubectl apply -f security/opa/ 2>/dev/null || true

workflows:
  security-workflow:
    jobs:
      - install-cert-manager:
          context: shopos-kubernetes
      - install-vault:
          requires: [install-cert-manager]
          context: shopos-kubernetes
      - install-keycloak:
          requires: [install-cert-manager]
          context: [shopos-kubernetes, shopos-keycloak]
      - install-kyverno:
          requires: [install-cert-manager]
          context: shopos-kubernetes
      - install-falco:
          requires: [install-cert-manager]
          context: [shopos-kubernetes, shopos-slack]
      - install-opa:
          requires: [install-cert-manager]
          context: shopos-kubernetes

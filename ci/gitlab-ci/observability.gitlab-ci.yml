# GitLab CI — Observability Stack with GitLab Environments
#
# GitLab CI role: deploy observability to named GitLab environments.
# Each tool gets its own environment: entry so GitLab tracks deployment history,
# provides environment URLs in the UI, and supports stop actions for cleanup.

variables:
  ENVIRONMENT: dev
  ACTION: INSTALL
  PROMETHEUS: "true"
  GRAFANA: "true"
  LOKI: "true"
  JAEGER: "true"
  TEMPO: "true"
  OTEL_COLLECTOR: "true"
  THANOS: "false"
  FLUENT_BIT: "true"
  METRICS_SERVER: "true"

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_PIPELINE_SOURCE == "trigger"

stages:
  - foundation
  - metrics
  - logs
  - traces
  - pipeline

default:
  image: alpine/helm:3.16.1
  before_script:
    - apk add --no-cache curl bash
    - curl -fsSL "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl" \
        -o /usr/local/bin/kubectl && chmod +x /usr/local/bin/kubectl
    - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config

metrics-server:
  stage: foundation
  environment:
    name: $ENVIRONMENT/observability/metrics-server
  script:
    - '[ "$METRICS_SERVER" = "true" ] || exit 0'
    - helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server --force-update
    - |
      [ "$ACTION" = "UNINSTALL" ] && helm uninstall metrics-server -n kube-system 2>/dev/null || true
      [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install metrics-server metrics-server/metrics-server \
        --namespace kube-system --set args[0]=--kubelet-insecure-tls --wait --timeout 3m

prometheus:
  stage: metrics
  environment:
    name: $ENVIRONMENT/observability/prometheus
    url: https://prometheus.$ENVIRONMENT.shopos.internal
    on_stop: stop-prometheus
  script:
    - '[ "$PROMETHEUS" = "true" ] || exit 0'
    - helm repo add prometheus-community https://prometheus-community.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall kube-prometheus-stack -n observability 2>/dev/null || true
      else
        RETENTION="15d"; [ "$ENVIRONMENT" = "staging" ] && RETENTION="30d"; [ "$ENVIRONMENT" = "prod" ] && RETENTION="90d"
        REPLICAS=1; [ "$ENVIRONMENT" != "dev" ] && REPLICAS=2
        helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
          --namespace observability --create-namespace \
          --set prometheus.prometheusSpec.retention=$RETENTION \
          --set prometheus.prometheusSpec.replicas=$REPLICAS \
          --set alertmanager.alertmanagerSpec.replicas=$REPLICAS \
          --set grafana.enabled=false \
          --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false \
          --wait --timeout 10m
        kubectl apply -f observability/prometheus/ 2>/dev/null || true
      fi
  needs: [metrics-server]

stop-prometheus:
  stage: metrics
  environment:
    name: $ENVIRONMENT/observability/prometheus
    action: stop
  script:
    - helm uninstall kube-prometheus-stack -n observability --ignore-not-found || true
  when: manual

grafana:
  stage: metrics
  environment:
    name: $ENVIRONMENT/observability/grafana
    url: https://grafana.$ENVIRONMENT.shopos.internal
  script:
    - '[ "$GRAFANA" = "true" ] || exit 0'
    - helm repo add grafana https://grafana.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall grafana -n observability 2>/dev/null || true
      else
        helm upgrade --install grafana grafana/grafana \
          --namespace observability --create-namespace \
          --set adminPassword="$GRAFANA_ADMIN_PASSWORD" \
          --set persistence.enabled=true \
          --set sidecar.dashboards.enabled=true \
          --set sidecar.datasources.enabled=true \
          --wait --timeout 5m
      fi
  needs: [metrics-server]

loki:
  stage: logs
  environment:
    name: $ENVIRONMENT/observability/loki
  script:
    - '[ "$LOKI" = "true" ] || exit 0'
    - helm repo add grafana https://grafana.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall loki -n observability 2>/dev/null || true
      else
        helm upgrade --install loki grafana/loki \
          --namespace observability --create-namespace \
          --set loki.auth_enabled=false \
          --set loki.storage.type=s3 \
          --set loki.storage.s3.endpoint="$MINIO_ENDPOINT" \
          --set loki.storage.s3.bucketnames=loki-chunks \
          --set loki.storage.s3.access_key_id="$MINIO_ACCESS_KEY" \
          --set loki.storage.s3.secret_access_key="$MINIO_SECRET_KEY" \
          --set loki.storage.s3.s3ForcePathStyle=true \
          --wait --timeout 8m
      fi
  needs: [prometheus, grafana]

fluent-bit:
  stage: logs
  environment:
    name: $ENVIRONMENT/observability/fluent-bit
  script:
    - '[ "$FLUENT_BIT" = "true" ] || exit 0'
    - helm repo add fluent https://fluent.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall fluent-bit -n observability 2>/dev/null || true
      else
        helm upgrade --install fluent-bit fluent/fluent-bit \
          --namespace observability --create-namespace \
          --wait --timeout 5m
      fi
  needs: [loki]

jaeger:
  stage: traces
  environment:
    name: $ENVIRONMENT/observability/jaeger
    url: https://jaeger.$ENVIRONMENT.shopos.internal
  script:
    - '[ "$JAEGER" = "true" ] || exit 0'
    - helm repo add jaegertracing https://jaegertracing.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall jaeger -n observability 2>/dev/null || true
      else
        helm upgrade --install jaeger jaegertracing/jaeger \
          --namespace observability --create-namespace \
          --set provisionDataStore.cassandra=false \
          --set storage.type=memory \
          --wait --timeout 8m
      fi
  needs: [prometheus, grafana]

tempo:
  stage: traces
  environment:
    name: $ENVIRONMENT/observability/tempo
  script:
    - '[ "$TEMPO" = "true" ] || exit 0'
    - helm repo add grafana https://grafana.github.io/helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall tempo-distributed -n observability 2>/dev/null || true
      else
        helm upgrade --install tempo-distributed grafana/tempo-distributed \
          --namespace observability --create-namespace \
          --set storage.trace.backend=s3 \
          --set storage.trace.s3.endpoint="$MINIO_ENDPOINT" \
          --set storage.trace.s3.bucket=tempo-traces \
          --set storage.trace.s3.access_key="$MINIO_ACCESS_KEY" \
          --set storage.trace.s3.secret_key="$MINIO_SECRET_KEY" \
          --set storage.trace.s3.insecure=true \
          --wait --timeout 8m
      fi
  needs: [prometheus, grafana]

otel-collector:
  stage: pipeline
  environment:
    name: $ENVIRONMENT/observability/otel-collector
  script:
    - '[ "$OTEL_COLLECTOR" = "true" ] || exit 0'
    - helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts --force-update
    - |
      if [ "$ACTION" = "UNINSTALL" ]; then
        helm uninstall otel-collector -n observability 2>/dev/null || true
      else
        helm upgrade --install otel-collector open-telemetry/opentelemetry-collector \
          --namespace observability --create-namespace \
          --set mode=deployment \
          --set replicaCount=2 \
          -f observability/otel-collector/values.yaml 2>/dev/null \
          --wait --timeout 5m
      fi
  needs: [jaeger, tempo, fluent-bit]

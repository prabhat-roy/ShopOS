# Observability Stack Pipeline — Harness CI
pipeline:
  identifier: shopos_observability
  name: ShopOS Observability Stack
  projectIdentifier: shopos
  orgIdentifier: default

  stages:
    - stage:
        identifier: metrics
        name: Metrics — Prometheus Stack
        type: CI
        spec:
          cloneCodebase: false
          execution:
            steps:
              - step:
                  identifier: prometheus
                  name: Install kube-prometheus-stack
                  type: Run
                  spec:
                    image: alpine/helm:latest
                    envVariables:
                      KUBECONFIG_CONTENT: <+secrets.getValue("kubeconfig_content")>
                    command: |
                      mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
                      helm repo add prometheus-community https://prometheus-community.github.io/helm-charts --force-update
                      helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
                        --namespace observability --create-namespace \
                        --set prometheus.prometheusSpec.retention=30d \
                        --set prometheus.prometheusSpec.replicas=2 \
                        --set grafana.enabled=false --wait --timeout 10m

    - stage:
        identifier: dashboards_logs
        name: Dashboards + Logs
        type: CI
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: grafana
                  name: Install Grafana
                  type: Run
                  spec:
                    image: alpine/helm:latest
                    envVariables:
                      KUBECONFIG_CONTENT: <+secrets.getValue("kubeconfig_content")>
                      GRAFANA_ADMIN_PASSWORD: <+secrets.getValue("grafana_admin_password")>
                    command: |
                      mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
                      helm repo add grafana https://grafana.github.io/helm-charts --force-update
                      helm upgrade --install grafana grafana/grafana \
                        --namespace observability \
                        --set adminPassword="$GRAFANA_ADMIN_PASSWORD" \
                        --set persistence.enabled=true --set replicas=2 \
                        --wait --timeout 5m
                      kubectl apply -f observability/grafana/ 2>/dev/null || true
              - step:
                  identifier: loki
                  name: Install Loki
                  type: Run
                  spec:
                    image: alpine/helm:latest
                    envVariables:
                      KUBECONFIG_CONTENT: <+secrets.getValue("kubeconfig_content")>
                      MINIO_SECRET_KEY: <+secrets.getValue("minio_secret_key")>
                    command: |
                      mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
                      helm repo add grafana https://grafana.github.io/helm-charts --force-update
                      helm upgrade --install loki grafana/loki \
                        --namespace observability \
                        --set loki.storage.type=s3 \
                        --set loki.storage.s3.endpoint="http://minio.registry.svc:9000" \
                        --set loki.storage.s3.bucketnames=loki-chunks \
                        --set loki.storage.s3.secretAccessKey="$MINIO_SECRET_KEY" \
                        --set loki.storage.s3.s3ForcePathStyle=true \
                        --set deploymentMode=SimpleScalable \
                        --wait --timeout 8m

    - stage:
        identifier: tracing
        name: Tracing — Jaeger + OTel
        type: CI
        spec:
          cloneCodebase: false
          execution:
            steps:
              - step:
                  identifier: jaeger
                  name: Install Jaeger
                  type: Run
                  spec:
                    image: alpine/helm:latest
                    envVariables:
                      KUBECONFIG_CONTENT: <+secrets.getValue("kubeconfig_content")>
                    command: |
                      mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
                      helm repo add jaegertracing https://jaegertracing.github.io/helm-charts --force-update
                      helm upgrade --install jaeger jaegertracing/jaeger \
                        --namespace observability \
                        --set provisionDataStore.cassandra=false \
                        --set collector.replicaCount=2 --set query.replicaCount=2 \
                        --wait --timeout 8m
              - step:
                  identifier: otel_collector
                  name: Install OTel Collector
                  type: Run
                  spec:
                    image: alpine/helm:latest
                    envVariables:
                      KUBECONFIG_CONTENT: <+secrets.getValue("kubeconfig_content")>
                    command: |
                      mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
                      helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts --force-update
                      helm upgrade --install otel-collector open-telemetry/opentelemetry-collector \
                        --namespace observability \
                        --set mode=daemonset \
                        --set config.exporters.otlp.endpoint=jaeger-collector.observability.svc:4317 \
                        --wait --timeout 5m

version: 0.2

# AWS CodePipeline — Deploy observability stack to EKS
# Loki/Tempo backed by S3 (AWS-native); metrics to CloudWatch + Prometheus
# ACTION: INSTALL | UPGRADE | UNINSTALL

env:
  parameter-store:
    EKS_CLUSTER_NAME: /shopos/eks/cluster_name
    AWS_REGION: /shopos/aws/region
    GRAFANA_ADMIN_PASSWORD: /shopos/grafana/admin-password
    S3_BUCKET_LOGS: /shopos/s3/logs-bucket
    S3_BUCKET_TRACES: /shopos/s3/traces-bucket
  variables:
    ENVIRONMENT: dev
    ACTION: INSTALL
    PROMETHEUS: "true"
    GRAFANA: "true"
    LOKI: "true"
    JAEGER: "true"
    TEMPO: "true"
    OTEL_COLLECTOR: "true"
    THANOS: "false"
    PYROSCOPE: "false"
    FLUENT_BIT: "true"
    METRICS_SERVER: "true"

phases:
  install:
    commands:
      - curl -fsSL "https://dl.k8s.io/release/$(curl -Ls https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl" -o /usr/local/bin/kubectl && chmod +x /usr/local/bin/kubectl
      - curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

  pre_build:
    commands:
      - aws eks update-kubeconfig --region ${AWS_REGION} --name ${EKS_CLUSTER_NAME}
      - echo "Observability ${ACTION} on ${ENVIRONMENT}"
      - |
        # Create S3 buckets for logs + traces if they don't exist
        if [ "${ACTION}" = "INSTALL" ]; then
          aws s3api create-bucket --bucket ${S3_BUCKET_LOGS} --region ${AWS_REGION} \
            --create-bucket-configuration LocationConstraint=${AWS_REGION} 2>/dev/null || true
          aws s3api create-bucket --bucket ${S3_BUCKET_TRACES} --region ${AWS_REGION} \
            --create-bucket-configuration LocationConstraint=${AWS_REGION} 2>/dev/null || true
        fi

  build:
    commands:
      - |
        if [ "${METRICS_SERVER}" = "true" ]; then
          helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall metrics-server -n kube-system 2>/dev/null || true
          else
            helm upgrade --install metrics-server metrics-server/metrics-server \
              --namespace kube-system \
              --set args[0]=--kubelet-insecure-tls \
              --wait --timeout 3m
          fi
        fi

      - |
        if [ "${PROMETHEUS}" = "true" ]; then
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall kube-prometheus-stack -n observability 2>/dev/null || true
          else
            helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
              --namespace observability --create-namespace \
              --set prometheus.prometheusSpec.retention=30d \
              --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.storageClassName=gp3 \
              --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.resources.requests.storage=50Gi \
              --set alertmanager.alertmanagerSpec.replicas=2 \
              --set grafana.enabled=false \
              --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false \
              --wait --timeout 10m
            kubectl apply -f observability/prometheus/ 2>/dev/null || true
          fi
        fi

      - |
        if [ "${GRAFANA}" = "true" ]; then
          helm repo add grafana https://grafana.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall grafana -n observability 2>/dev/null || true
          else
            helm upgrade --install grafana grafana/grafana \
              --namespace observability --create-namespace \
              --set adminPassword="${GRAFANA_ADMIN_PASSWORD}" \
              --set persistence.enabled=true \
              --set persistence.storageClassName=gp3 \
              --set sidecar.dashboards.enabled=true \
              --set sidecar.datasources.enabled=true \
              --wait --timeout 5m
            kubectl apply -f observability/grafana/ 2>/dev/null || true
          fi
        fi

      - |
        if [ "${LOKI}" = "true" ]; then
          helm repo add grafana https://grafana.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall loki -n observability 2>/dev/null || true
          else
            helm upgrade --install loki grafana/loki \
              --namespace observability --create-namespace \
              --set loki.auth_enabled=false \
              --set loki.storage.type=s3 \
              --set loki.storage.s3.region=${AWS_REGION} \
              --set loki.storage.s3.bucketnames=${S3_BUCKET_LOGS} \
              --set loki.storage.s3.s3ForcePathStyle=false \
              --wait --timeout 8m
          fi
        fi

      - |
        if [ "${TEMPO}" = "true" ]; then
          helm repo add grafana https://grafana.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall tempo-distributed -n observability 2>/dev/null || true
          else
            helm upgrade --install tempo-distributed grafana/tempo-distributed \
              --namespace observability --create-namespace \
              --set storage.trace.backend=s3 \
              --set storage.trace.s3.region=${AWS_REGION} \
              --set storage.trace.s3.bucket=${S3_BUCKET_TRACES} \
              --wait --timeout 8m
          fi
        fi

      - |
        if [ "${JAEGER}" = "true" ]; then
          helm repo add jaegertracing https://jaegertracing.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall jaeger -n observability 2>/dev/null || true
          else
            helm upgrade --install jaeger jaegertracing/jaeger \
              --namespace observability --create-namespace \
              --set provisionDataStore.cassandra=false \
              --set allInOne.enabled=true \
              --wait --timeout 8m
          fi
        fi

      - |
        if [ "${OTEL_COLLECTOR}" = "true" ]; then
          helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall otel-collector -n observability 2>/dev/null || true
          else
            helm upgrade --install otel-collector open-telemetry/opentelemetry-collector \
              --namespace observability --create-namespace \
              --set mode=deployment --set replicaCount=2 \
              -f observability/otel-collector/values.yaml \
              --wait --timeout 5m
          fi
        fi

      - |
        if [ "${FLUENT_BIT}" = "true" ]; then
          helm repo add fluent https://fluent.github.io/helm-charts --force-update
          if [ "${ACTION}" = "UNINSTALL" ]; then
            helm uninstall fluent-bit -n observability 2>/dev/null || true
          else
            helm upgrade --install fluent-bit fluent/fluent-bit \
              --namespace observability --create-namespace \
              --wait --timeout 5m
          fi
        fi

  post_build:
    commands:
      - echo "Observability ${ACTION} complete on ${ENVIRONMENT}"
      - kubectl get pods -n observability 2>/dev/null | head -20 || true
      - |
        aws cloudwatch put-metric-data \
          --metric-name ObservabilityDeployed \
          --namespace ShopOS/CI \
          --value 1 --unit Count \
          --region ${AWS_REGION} || true

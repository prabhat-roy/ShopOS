# GCP Cloud Build — Deploy observability stack to GKE
# Loki/Tempo backed by GCS; metrics via Cloud Monitoring integration
# _ACTION: INSTALL | UPGRADE | UNINSTALL

substitutions:
  _ENVIRONMENT: dev
  _CLUSTER_NAME: shopos-dev
  _REGION: us-central1
  _ACTION: INSTALL
  _GCS_BUCKET_LOGS: shopos-loki-logs
  _GCS_BUCKET_TRACES: shopos-tempo-traces
  _PROMETHEUS: "true"
  _GRAFANA: "true"
  _LOKI: "true"
  _JAEGER: "true"
  _TEMPO: "true"
  _OTEL_COLLECTOR: "true"
  _FLUENT_BIT: "true"
  _METRICS_SERVER: "true"

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_STANDARD_4

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/shopos-grafana-admin-password/versions/latest
      env: GRAFANA_ADMIN_PASSWORD

steps:
  - name: gcr.io/cloud-builders/gcloud
    id: get-credentials
    args:
      - container
      - clusters
      - get-credentials
      - ${_CLUSTER_NAME}
      - --region=${_REGION}
      - --project=${PROJECT_ID}

  - name: gcr.io/cloud-builders/gcloud
    id: ensure-gcs-buckets
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_ACTION}" = "INSTALL" ] || exit 0
        gcloud storage buckets create gs://${_GCS_BUCKET_LOGS} \
          --project=${PROJECT_ID} --location=${_REGION} 2>/dev/null || true
        gcloud storage buckets create gs://${_GCS_BUCKET_TRACES} \
          --project=${PROJECT_ID} --location=${_REGION} 2>/dev/null || true
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: metrics-server
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_METRICS_SERVER}" = "true" ] || exit 0
        helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall metrics-server -n kube-system 2>/dev/null || true
        else
          helm upgrade --install metrics-server metrics-server/metrics-server \
            --namespace kube-system \
            --set args[0]=--kubelet-insecure-tls \
            --wait --timeout 3m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: prometheus
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_PROMETHEUS}" = "true" ] || exit 0
        helm repo add prometheus-community https://prometheus-community.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall kube-prometheus-stack -n observability 2>/dev/null || true
        else
          helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
            --namespace observability --create-namespace \
            --set prometheus.prometheusSpec.retention=30d \
            --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.storageClassName=standard-rwo \
            --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.resources.requests.storage=50Gi \
            --set alertmanager.alertmanagerSpec.replicas=2 \
            --set grafana.enabled=false \
            --set prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false \
            --wait --timeout 10m
          kubectl apply -f observability/prometheus/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [metrics-server]

  - name: alpine/helm:3.16.0
    id: grafana
    secretEnv: [GRAFANA_ADMIN_PASSWORD]
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_GRAFANA}" = "true" ] || exit 0
        helm repo add grafana https://grafana.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall grafana -n observability 2>/dev/null || true
        else
          helm upgrade --install grafana grafana/grafana \
            --namespace observability --create-namespace \
            --set adminPassword="$$GRAFANA_ADMIN_PASSWORD" \
            --set persistence.enabled=true \
            --set persistence.storageClassName=standard-rwo \
            --set sidecar.dashboards.enabled=true \
            --set sidecar.datasources.enabled=true \
            --wait --timeout 5m
          kubectl apply -f observability/grafana/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [prometheus]

  - name: alpine/helm:3.16.0
    id: loki
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_LOKI}" = "true" ] || exit 0
        helm repo add grafana https://grafana.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall loki -n observability 2>/dev/null || true
        else
          # Loki with GCS backend
          helm upgrade --install loki grafana/loki \
            --namespace observability --create-namespace \
            --set loki.auth_enabled=false \
            --set loki.storage.type=gcs \
            --set loki.storage.gcs.bucket_name=${_GCS_BUCKET_LOGS} \
            --wait --timeout 8m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [ensure-gcs-buckets]

  - name: alpine/helm:3.16.0
    id: tempo
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_TEMPO}" = "true" ] || exit 0
        helm repo add grafana https://grafana.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall tempo-distributed -n observability 2>/dev/null || true
        else
          # Tempo with GCS backend
          helm upgrade --install tempo-distributed grafana/tempo-distributed \
            --namespace observability --create-namespace \
            --set storage.trace.backend=gcs \
            --set storage.trace.gcs.bucket_name=${_GCS_BUCKET_TRACES} \
            --wait --timeout 8m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [ensure-gcs-buckets]

  - name: alpine/helm:3.16.0
    id: jaeger
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_JAEGER}" = "true" ] || exit 0
        helm repo add jaegertracing https://jaegertracing.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall jaeger -n observability 2>/dev/null || true
        else
          helm upgrade --install jaeger jaegertracing/jaeger \
            --namespace observability --create-namespace \
            --set provisionDataStore.cassandra=false \
            --set allInOne.enabled=true \
            --wait --timeout 8m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: otel-collector
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_OTEL_COLLECTOR}" = "true" ] || exit 0
        helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall otel-collector -n observability 2>/dev/null || true
        else
          helm upgrade --install otel-collector open-telemetry/opentelemetry-collector \
            --namespace observability --create-namespace \
            --set mode=deployment --set replicaCount=2 \
            -f observability/otel-collector/values.yaml \
            --wait --timeout 5m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: fluent-bit
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_FLUENT_BIT}" = "true" ] || exit 0
        helm repo add fluent https://fluent.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall fluent-bit -n observability 2>/dev/null || true
        else
          helm upgrade --install fluent-bit fluent/fluent-bit \
            --namespace observability --create-namespace \
            --wait --timeout 5m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

# GCP Cloud Build — Deploy security stack to GKE
# Vault backed by Google Cloud KMS auto-unseal; secrets from Secret Manager
# _ACTION: INSTALL | UPGRADE | UNINSTALL

substitutions:
  _ENVIRONMENT: dev
  _CLUSTER_NAME: shopos-dev
  _REGION: us-central1
  _ACTION: INSTALL
  _CERT_MANAGER: "true"
  _VAULT: "true"
  _KEYCLOAK: "true"
  _SPIRE: "true"
  _OPA: "true"
  _KYVERNO: "true"
  _KUBEWARDEN: "true"
  _FALCO: "true"
  _TRIVY_OPERATOR: "true"

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_STANDARD_4

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/shopos-keycloak-admin-password/versions/latest
      env: KEYCLOAK_ADMIN_PASSWORD
    - versionName: projects/$PROJECT_ID/secrets/shopos-vault-gcp-kms-key/versions/latest
      env: VAULT_GCP_KMS_KEY

steps:
  - name: gcr.io/cloud-builders/gcloud
    id: get-credentials
    args:
      - container
      - clusters
      - get-credentials
      - ${_CLUSTER_NAME}
      - --region=${_REGION}
      - --project=${PROJECT_ID}

  - name: alpine/helm:3.16.0
    id: cert-manager
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_CERT_MANAGER}" = "true" ] || exit 0
        helm repo add jetstack https://charts.jetstack.io --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall cert-manager -n cert-manager 2>/dev/null || true
        else
          helm upgrade --install cert-manager jetstack/cert-manager \
            --namespace cert-manager --create-namespace \
            --set installCRDs=true --set replicaCount=2 \
            --wait --timeout 5m
          kubectl apply -f security/cert-manager/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: vault
    secretEnv: [VAULT_GCP_KMS_KEY]
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_VAULT}" = "true" ] || exit 0
        helm repo add hashicorp https://helm.releases.hashicorp.com --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall vault -n vault 2>/dev/null || true
        else
          # Vault with Google Cloud KMS auto-unseal
          helm upgrade --install vault hashicorp/vault \
            --namespace vault --create-namespace \
            --set server.ha.enabled=true \
            --set server.ha.raft.enabled=true \
            --set server.ha.replicas=3 \
            --set ui.enabled=true \
            --set "server.extraEnvironmentVars.VAULT_SEAL_TYPE=gcpckms" \
            --set "server.extraEnvironmentVars.VAULT_GCPCKMS_SEAL_KEY_RING=shopos-vault" \
            --set "server.extraEnvironmentVars.VAULT_GCPCKMS_SEAL_CRYPTO_KEY=vault-unseal" \
            --set "server.extraEnvironmentVars.VAULT_GCPCKMS_SEAL_PROJECT=${PROJECT_ID}" \
            --set "server.extraEnvironmentVars.VAULT_GCPCKMS_SEAL_REGION=${_REGION}" \
            --wait --timeout 10m
          kubectl apply -f security/vault/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: keycloak
    secretEnv: [KEYCLOAK_ADMIN_PASSWORD]
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_KEYCLOAK}" = "true" ] || exit 0
        helm repo add bitnami https://charts.bitnami.com/bitnami --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall keycloak -n keycloak 2>/dev/null || true
        else
          helm upgrade --install keycloak bitnami/keycloak \
            --namespace keycloak --create-namespace \
            --set replicaCount=2 \
            --set auth.adminUser=admin \
            --set auth.adminPassword="$$KEYCLOAK_ADMIN_PASSWORD" \
            --set postgresql.enabled=true \
            --set postgresql.primary.persistence.storageClass=standard-rwo \
            --wait --timeout 10m
          kubectl apply -f security/keycloak/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: kyverno
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_KYVERNO}" = "true" ] || exit 0
        helm repo add kyverno https://kyverno.github.io/kyverno --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall kyverno -n kyverno 2>/dev/null || true
        else
          helm upgrade --install kyverno kyverno/kyverno \
            --namespace kyverno --create-namespace \
            --set replicaCount=2 --wait --timeout 5m
          kubectl apply -f security/kyverno/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: opa
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_OPA}" = "true" ] || exit 0
        helm repo add gatekeeper https://open-policy-agent.github.io/gatekeeper/charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall gatekeeper -n gatekeeper-system 2>/dev/null || true
        else
          helm upgrade --install gatekeeper gatekeeper/gatekeeper \
            --namespace gatekeeper-system --create-namespace \
            --set replicas=2 --set auditInterval=30 \
            --wait --timeout 5m
          kubectl apply -f security/opa/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: falco
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_FALCO}" = "true" ] || exit 0
        helm repo add falcosecurity https://falcosecurity.github.io/charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall falco -n falco 2>/dev/null || true
        else
          helm upgrade --install falco falcosecurity/falco \
            --namespace falco --create-namespace \
            --set driver.kind=ebpf \
            --set falcosidekick.enabled=true \
            --wait --timeout 8m
          kubectl apply -f security/falco/ 2>/dev/null || true
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

  - name: alpine/helm:3.16.0
    id: trivy-operator
    entrypoint: bash
    args:
      - -c
      - |
        [ "${_TRIVY_OPERATOR}" = "true" ] || exit 0
        helm repo add aqua https://aquasecurity.github.io/helm-charts --force-update
        if [ "${_ACTION}" = "UNINSTALL" ]; then
          helm uninstall trivy-operator -n trivy-system 2>/dev/null || true
        else
          helm upgrade --install trivy-operator aqua/trivy-operator \
            --namespace trivy-system --create-namespace \
            --set trivy.ignoreUnfixed=true \
            --wait --timeout 5m
        fi
    env:
      - CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}
      - CLOUDSDK_COMPUTE_REGION=${_REGION}
    waitFor: [get-credentials]

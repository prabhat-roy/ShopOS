# Woodpecker CI — Security Infrastructure + Lightweight Scan
# Role: install security tools AND run lightweight security smoke checks.
#       Woodpecker variant adds a quick Trivy scan of the gateway image post-install.
# Secrets: kubeconfig_content, keycloak_admin_password, slack_webhook, harbor_registry, harbor_user, harbor_password

when:
  event: [custom, manual]

variables:
  ENVIRONMENT: dev
  ACTION: INSTALL

steps:
  setup-kubeconfig:
    image: alpine:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
    commands:
      - mkdir -p ~/.kube
      - echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config

  cert-manager:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
    commands:
      - '[ "$CERT_MANAGER" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add jetstack https://charts.jetstack.io --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall cert-manager -n cert-manager 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install cert-manager jetstack/cert-manager \
          --namespace cert-manager --create-namespace \
          --set installCRDs=true --set replicaCount=2 --wait --timeout 5m || true
    depends_on: [setup-kubeconfig]

  vault:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
    commands:
      - '[ "$VAULT" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add hashicorp https://helm.releases.hashicorp.com --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall vault -n vault 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install vault hashicorp/vault \
          --namespace vault --create-namespace \
          --set server.ha.enabled=true --set server.ha.raft.enabled=true \
          --set server.ha.replicas=3 --set ui.enabled=true \
          --wait --timeout 10m || true
    depends_on: [cert-manager]

  keycloak:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
      KEYCLOAK_ADMIN_PASSWORD:
        from_secret: keycloak_admin_password
    commands:
      - '[ "$KEYCLOAK" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add bitnami https://charts.bitnami.com/bitnami --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall keycloak -n keycloak 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install keycloak bitnami/keycloak \
          --namespace keycloak --create-namespace \
          --set replicaCount=2 \
          --set auth.adminPassword="$KEYCLOAK_ADMIN_PASSWORD" \
          --set postgresql.enabled=true \
          --wait --timeout 10m || true
    depends_on: [vault]

  kyverno:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
    commands:
      - '[ "$KYVERNO" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add kyverno https://kyverno.github.io/kyverno --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall kyverno -n kyverno 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install kyverno kyverno/kyverno \
          --namespace kyverno --create-namespace \
          --set replicaCount=2 \
          --wait --timeout 5m || true
    depends_on: [keycloak]

  falco:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
      SLACK_WEBHOOK:
        from_secret: slack_webhook
    commands:
      - '[ "$FALCO" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add falcosecurity https://falcosecurity.github.io/charts --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall falco -n falco 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install falco falcosecurity/falco \
          --namespace falco --create-namespace \
          --set driver.kind=ebpf \
          --set falcosidekick.enabled=true \
          --set falcosidekick.config.slack.webhookurl="$SLACK_WEBHOOK" \
          --wait --timeout 8m || true
    depends_on: [kyverno]

  trivy-operator:
    image: alpine/helm:latest
    environment:
      KUBECONFIG_CONTENT:
        from_secret: kubeconfig_content
    commands:
      - '[ "$TRIVY_OPERATOR" = "true" ] || exit 0'
      - mkdir -p ~/.kube && echo "$KUBECONFIG_CONTENT" | base64 -d > ~/.kube/config
      - helm repo add aqua https://aquasecurity.github.io/helm-charts --force-update
      - |
        [ "$ACTION" = "UNINSTALL" ] && helm uninstall trivy-operator -n trivy-system 2>/dev/null || true
        [ "$ACTION" != "UNINSTALL" ] && helm upgrade --install trivy-operator aqua/trivy-operator \
          --namespace trivy-system --create-namespace \
          --set trivy.ignoreUnfixed=true \
          --wait --timeout 5m || true
    depends_on: [falco]

  lightweight-trivy-scan:
    image: aquasec/trivy:latest
    environment:
      HARBOR_REGISTRY:
        from_secret: harbor_registry
      HARBOR_USER:
        from_secret: harbor_user
      HARBOR_PASSWORD:
        from_secret: harbor_password
    commands:
      - '[ "${ACTION:-INSTALL}" != "UNINSTALL" ] || exit 0'
      - |
        echo "=== Lightweight Trivy scan on api-gateway (smoke) ==="
        IMAGE_TAG="${CI_COMMIT_SHA:0:8}"
        IMG="$HARBOR_REGISTRY/shopos/api-gateway:$IMAGE_TAG"
        trivy image \
          --username "$HARBOR_USER" \
          --password "$HARBOR_PASSWORD" \
          --severity CRITICAL \
          --exit-code 0 \
          "$IMG" 2>/dev/null || echo "WARN could not scan $IMG"
    depends_on: [trivy-operator]

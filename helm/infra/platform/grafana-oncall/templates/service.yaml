apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-oncall.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grafana-oncall.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-oncall.selectorLabels" . | nindent 4 }}

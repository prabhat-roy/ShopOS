apiVersion: v1
kind: Secret
metadata:
  name: {{ include "boundary.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "boundary.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ .Values.config.database.url | quote }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-camel.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "apache-camel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache-camel.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bytebase.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "bytebase.labels" . | nindent 4 }}
type: Opaque
stringData:
  pg-url: {{ .Values.database.url | quote }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "marquez-web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "marquez-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marquez-web.selectorLabels" . | nindent 4 }}

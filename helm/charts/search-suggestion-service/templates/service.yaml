apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-suggestion-service.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "search-suggestion-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: grpc
  selector:
    {{- include "search-suggestion-service.selectorLabels" . | nindent 4 }}

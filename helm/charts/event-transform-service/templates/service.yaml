apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-transform-service.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "event-transform-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: grpc
  selector:
    {{- include "event-transform-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{- include "route-optimization-service.fullname" . }}
  namespace: {{- .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "route-optimization-service.labels" . | nindent 4 }}
spec:
  type: {{- .Values.service.type }}
  selector:
    {{- include "route-optimization-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{- .Values.service.port }}
      targetPort: {{- .Values.service.targetPort }}
      protocol: {{- .Values.service.protocol }}

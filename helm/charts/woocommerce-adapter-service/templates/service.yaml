apiVersion: v1
kind: Service
metadata:
  name: {{ include "woocommerce-adapter-service.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "woocommerce-adapter-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: http
  selector:
    {{- include "woocommerce-adapter-service.selectorLabels" . | nindent 4 }}

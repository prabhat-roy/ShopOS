apiVersion: v1
kind: Service
metadata:
  name: {{ include "tax-exemption-service.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "tax-exemption-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: grpc
  selector:
    {{- include "tax-exemption-service.selectorLabels" . | nindent 4 }}

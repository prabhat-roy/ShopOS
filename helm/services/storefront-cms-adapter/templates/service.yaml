apiVersion: v1
kind: Service
metadata:
  name: {{ include "storefront-cms-adapter.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "storefront-cms-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: http
  selector:
    {{- include "storefront-cms-adapter.selectorLabels" . | nindent 4 }}

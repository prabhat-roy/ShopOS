apiVersion: v1
kind: Service
metadata:
  name: {{ include "pubsub-router-service.fullname" . }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "pubsub-router-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: grpc
  selector:
    {{- include "pubsub-router-service.selectorLabels" . | nindent 4 }}

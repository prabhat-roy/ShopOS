# Static inventory — used for local/manual runs
# For cloud deployments use the dynamic inventories: aws.yml, gcp.yml, azure.yml

[jenkins]
# jenkins ansible_host=<PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[k8s_nodes]
# node1 ansible_host=10.0.1.10 ansible_user=ubuntu
# node2 ansible_host=10.0.1.11 ansible_user=ubuntu

[all:vars]
ansible_python_interpreter=/usr/bin/python3

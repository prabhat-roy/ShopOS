# Zipkin runs with default configuration
# Traces are stored in-memory (dev) or Elasticsearch (prod)
STORAGE_TYPE: mem
QUERY_PORT: 9411
COLLECTOR_ZIPKIN_HTTP_PORT: 9411

# For production with Elasticsearch backend:
# STORAGE_TYPE: elasticsearch
# ES_HOSTS: http://elasticsearch:9200
# ES_INDEX: zipkin

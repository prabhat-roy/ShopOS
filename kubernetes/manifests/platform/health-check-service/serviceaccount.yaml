apiVersion: v1
kind: ServiceAccount
metadata:
  name: health-check-service
  namespace: platform
  labels:
    app: health-check-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-health-check-service

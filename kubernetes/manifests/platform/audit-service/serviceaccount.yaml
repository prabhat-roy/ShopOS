apiVersion: v1
kind: ServiceAccount
metadata:
  name: audit-service
  namespace: platform
  labels:
    app: audit-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-audit-service

apiVersion: v1
kind: Service
metadata:
  name: event-store-service
  namespace: platform
  labels:
    app: event-store-service
    domain: platform
spec:
  selector:
    app: event-store-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50055
      targetPort: grpc
      protocol: TCP

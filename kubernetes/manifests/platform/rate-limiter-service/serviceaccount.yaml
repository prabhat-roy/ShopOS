apiVersion: v1
kind: ServiceAccount
metadata:
  name: rate-limiter-service
  namespace: platform
  labels:
    app: rate-limiter-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-rate-limiter-service

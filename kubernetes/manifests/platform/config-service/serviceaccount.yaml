apiVersion: v1
kind: ServiceAccount
metadata:
  name: config-service
  namespace: platform
  labels:
    app: config-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-config-service

apiVersion: v1
kind: Service
metadata:
  name: load-generator
  namespace: platform
  labels:
    app: load-generator
    domain: platform
spec:
  selector:
    app: load-generator
  type: ClusterIP
  ports:
    - name: http
      port: 8089
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: ServiceAccount
metadata:
  name: feature-flag-service
  namespace: platform
  labels:
    app: feature-flag-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-feature-flag-service

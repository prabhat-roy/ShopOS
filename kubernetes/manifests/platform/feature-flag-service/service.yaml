apiVersion: v1
kind: Service
metadata:
  name: feature-flag-service
  namespace: platform
  labels:
    app: feature-flag-service
    domain: platform
spec:
  selector:
    app: feature-flag-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
      protocol: TCP

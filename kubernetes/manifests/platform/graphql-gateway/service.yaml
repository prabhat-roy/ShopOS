apiVersion: v1
kind: Service
metadata:
  name: graphql-gateway
  namespace: platform
  labels:
    app: graphql-gateway
    domain: platform
spec:
  selector:
    app: graphql-gateway
  type: ClusterIP
  ports:
    - name: http
      port: 8086
      targetPort: http
      protocol: TCP

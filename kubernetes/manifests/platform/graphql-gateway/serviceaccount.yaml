apiVersion: v1
kind: ServiceAccount
metadata:
  name: graphql-gateway
  namespace: platform
  labels:
    app: graphql-gateway
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-graphql-gateway

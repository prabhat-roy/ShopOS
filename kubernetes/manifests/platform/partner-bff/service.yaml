apiVersion: v1
kind: Service
metadata:
  name: partner-bff
  namespace: platform
  labels:
    app: partner-bff
    domain: platform
spec:
  selector:
    app: partner-bff
  type: ClusterIP
  ports:
    - name: http
      port: 8083
      targetPort: http
      protocol: TCP

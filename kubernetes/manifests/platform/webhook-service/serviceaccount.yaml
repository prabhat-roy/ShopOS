apiVersion: v1
kind: ServiceAccount
metadata:
  name: webhook-service
  namespace: platform
  labels:
    app: webhook-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-webhook-service

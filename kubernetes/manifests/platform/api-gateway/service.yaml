apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: platform
  labels:
    app: api-gateway
    domain: platform
spec:
  selector:
    app: api-gateway
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

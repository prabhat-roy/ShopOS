apiVersion: v1
kind: ServiceAccount
metadata:
  name: scheduler-service
  namespace: platform
  labels:
    app: scheduler-service
    domain: platform
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-scheduler-service

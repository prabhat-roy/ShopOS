apiVersion: v1
kind: ServiceAccount
metadata:
  name: digest-service
  namespace: communications
  labels:
    app: digest-service
    domain: communications
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-digest-service

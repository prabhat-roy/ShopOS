apiVersion: v1
kind: ServiceAccount
metadata:
  name: chatbot-service
  namespace: communications
  labels:
    app: chatbot-service
    domain: communications
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-chatbot-service

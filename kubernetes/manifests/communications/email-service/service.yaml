apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: communications
  labels:
    app: email-service
    domain: communications
spec:
  selector:
    app: email-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

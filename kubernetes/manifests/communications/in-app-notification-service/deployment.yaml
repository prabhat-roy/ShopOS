apiVersion: apps/v1
kind: Deployment
metadata:
  name: in-app-notification-service
  namespace: communications
  labels:
    app: in-app-notification-service
    domain: communications
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: in-app-notification-service
  template:
    metadata:
      labels:
        app: in-app-notification-service
        domain: communications
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50132"
    spec:
      serviceAccountName: in-app-notification-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: in-app-notification-service
          image: shopos/in-app-notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50132
              protocol: TCP
          envFrom:
            - configMapRef:
                name: in-app-notification-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50132
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50132
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: in-app-notification-service

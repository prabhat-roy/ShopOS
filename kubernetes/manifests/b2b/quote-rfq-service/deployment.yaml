apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote-rfq-service
  namespace: b2b
  labels:
    app: quote-rfq-service
    domain: b2b
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quote-rfq-service
  template:
    metadata:
      labels:
        app: quote-rfq-service
        domain: b2b
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50162"
    spec:
      serviceAccountName: quote-rfq-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: quote-rfq-service
          image: shopos/quote-rfq-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50162
              protocol: TCP
          envFrom:
            - configMapRef:
                name: quote-rfq-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50162
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50162
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: quote-rfq-service

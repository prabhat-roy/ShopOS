apiVersion: v1
kind: ServiceAccount
metadata:
  name: organization-service
  namespace: b2b
  labels:
    app: organization-service
    domain: b2b
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-organization-service

apiVersion: v1
kind: Service
metadata:
  name: organization-service
  namespace: b2b
  labels:
    app: organization-service
    domain: b2b
spec:
  selector:
    app: organization-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50160
      targetPort: grpc
      protocol: TCP

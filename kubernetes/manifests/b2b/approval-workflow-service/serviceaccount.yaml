apiVersion: v1
kind: ServiceAccount
metadata:
  name: approval-workflow-service
  namespace: b2b
  labels:
    app: approval-workflow-service
    domain: b2b
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-approval-workflow-service

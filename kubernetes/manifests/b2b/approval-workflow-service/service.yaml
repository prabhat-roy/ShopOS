apiVersion: v1
kind: Service
metadata:
  name: approval-workflow-service
  namespace: b2b
  labels:
    app: approval-workflow-service
    domain: b2b
spec:
  selector:
    app: approval-workflow-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50163
      targetPort: grpc
      protocol: TCP

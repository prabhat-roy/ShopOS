apiVersion: v1
kind: Service
metadata:
  name: contract-service
  namespace: b2b
  labels:
    app: contract-service
    domain: b2b
spec:
  selector:
    app: contract-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50161
      targetPort: grpc
      protocol: TCP

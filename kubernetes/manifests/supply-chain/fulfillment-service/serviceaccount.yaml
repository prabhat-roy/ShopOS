apiVersion: v1
kind: ServiceAccount
metadata:
  name: fulfillment-service
  namespace: supply-chain
  labels:
    app: fulfillment-service
    domain: supply-chain
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-fulfillment-service

apiVersion: v1
kind: ServiceAccount
metadata:
  name: cold-chain-service
  namespace: supply-chain
  labels:
    app: cold-chain-service
    domain: supply-chain
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-cold-chain-service

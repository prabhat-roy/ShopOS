apiVersion: v1
kind: ServiceAccount
metadata:
  name: vendor-service
  namespace: supply-chain
  labels:
    app: vendor-service
    domain: supply-chain
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-vendor-service

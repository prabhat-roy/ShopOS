apiVersion: v1
kind: Service
metadata:
  name: warehouse-service
  namespace: supply-chain
  labels:
    app: warehouse-service
    domain: supply-chain
spec:
  selector:
    app: warehouse-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50102
      targetPort: grpc
      protocol: TCP

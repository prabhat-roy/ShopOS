apiVersion: v1
kind: Service
metadata:
  name: personalization-service
  namespace: analytics-ai
  labels:
    app: personalization-service
    domain: analytics-ai
spec:
  selector:
    app: personalization-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50153
      targetPort: grpc
      protocol: TCP

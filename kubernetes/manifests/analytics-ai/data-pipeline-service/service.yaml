apiVersion: v1
kind: Service
metadata:
  name: data-pipeline-service
  namespace: analytics-ai
  labels:
    app: data-pipeline-service
    domain: analytics-ai
spec:
  selector:
    app: data-pipeline-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

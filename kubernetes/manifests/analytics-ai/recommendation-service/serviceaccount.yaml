apiVersion: v1
kind: ServiceAccount
metadata:
  name: recommendation-service
  namespace: analytics-ai
  labels:
    app: recommendation-service
    domain: analytics-ai
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-recommendation-service

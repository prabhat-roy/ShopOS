apiVersion: v1
kind: Service
metadata:
  name: event-tracking-service
  namespace: analytics-ai
  labels:
    app: event-tracking-service
    domain: analytics-ai
spec:
  selector:
    app: event-tracking-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

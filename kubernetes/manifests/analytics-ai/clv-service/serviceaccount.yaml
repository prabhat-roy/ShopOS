apiVersion: v1
kind: ServiceAccount
metadata:
  name: clv-service
  namespace: analytics-ai
  labels:
    app: clv-service
    domain: analytics-ai
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-clv-service

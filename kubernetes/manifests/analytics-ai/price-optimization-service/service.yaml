apiVersion: v1
kind: Service
metadata:
  name: price-optimization-service
  namespace: analytics-ai
  labels:
    app: price-optimization-service
    domain: analytics-ai
spec:
  selector:
    app: price-optimization-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50151
      targetPort: grpc
      protocol: TCP

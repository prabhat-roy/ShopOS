apiVersion: v1
kind: ServiceAccount
metadata:
  name: ml-feature-store
  namespace: analytics-ai
  labels:
    app: ml-feature-store
    domain: analytics-ai
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-ml-feature-store

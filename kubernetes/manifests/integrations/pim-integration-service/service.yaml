apiVersion: v1
kind: Service
metadata:
  name: pim-integration-service
  namespace: integrations
  labels:
    app: pim-integration-service
    domain: integrations
spec:
  selector:
    app: pim-integration-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50195
      targetPort: grpc
      protocol: TCP

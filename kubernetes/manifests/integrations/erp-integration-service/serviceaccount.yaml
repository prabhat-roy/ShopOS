apiVersion: v1
kind: ServiceAccount
metadata:
  name: erp-integration-service
  namespace: integrations
  labels:
    app: erp-integration-service
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-erp-integration-service

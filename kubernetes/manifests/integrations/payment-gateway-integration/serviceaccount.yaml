apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-gateway-integration
  namespace: integrations
  labels:
    app: payment-gateway-integration
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-payment-gateway-integration

apiVersion: v1
kind: ServiceAccount
metadata:
  name: tax-provider-integration
  namespace: integrations
  labels:
    app: tax-provider-integration
    domain: integrations
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-tax-provider-integration

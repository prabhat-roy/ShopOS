apiVersion: v1
kind: Service
metadata:
  name: qa-service
  namespace: customer-experience
  labels:
    app: qa-service
    domain: customer-experience
spec:
  selector:
    app: qa-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50121
      targetPort: grpc
      protocol: TCP

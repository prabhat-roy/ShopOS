apiVersion: v1
kind: ServiceAccount
metadata:
  name: wishlist-service
  namespace: customer-experience
  labels:
    app: wishlist-service
    domain: customer-experience
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-wishlist-service

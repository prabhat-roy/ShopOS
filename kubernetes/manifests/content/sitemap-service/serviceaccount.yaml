apiVersion: v1
kind: ServiceAccount
metadata:
  name: sitemap-service
  namespace: content
  labels:
    app: sitemap-service
    domain: content
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-sitemap-service

apiVersion: v1
kind: Service
metadata:
  name: sitemap-service
  namespace: content
  labels:
    app: sitemap-service
    domain: content
spec:
  selector:
    app: sitemap-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50145
      targetPort: grpc
      protocol: TCP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sitemap-service
  namespace: content
  labels:
    app: sitemap-service
    domain: content
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sitemap-service
  template:
    metadata:
      labels:
        app: sitemap-service
        domain: content
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50145"
    spec:
      serviceAccountName: sitemap-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: sitemap-service
          image: shopos/sitemap-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50145
              protocol: TCP
          envFrom:
            - configMapRef:
                name: sitemap-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: 50145
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50145
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: sitemap-service

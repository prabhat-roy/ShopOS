apiVersion: v1
kind: Service
metadata:
  name: i18n-l10n-service
  namespace: content
  labels:
    app: i18n-l10n-service
    domain: content
spec:
  selector:
    app: i18n-l10n-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50146
      targetPort: grpc
      protocol: TCP

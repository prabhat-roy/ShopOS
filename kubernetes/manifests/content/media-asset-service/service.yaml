apiVersion: v1
kind: Service
metadata:
  name: media-asset-service
  namespace: content
  labels:
    app: media-asset-service
    domain: content
spec:
  selector:
    app: media-asset-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50140
      targetPort: grpc
      protocol: TCP

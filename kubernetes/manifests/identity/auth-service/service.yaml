apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: identity
  labels:
    app: auth-service
    domain: identity
spec:
  selector:
    app: auth-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50060
      targetPort: grpc
      protocol: TCP

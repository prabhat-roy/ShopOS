apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth-service
  namespace: identity
  labels:
    app: auth-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-auth-service

apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: identity
  labels:
    app: user-service
    domain: identity
spec:
  selector:
    app: user-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50061
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service
  namespace: identity
  labels:
    app: user-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-user-service

apiVersion: v1
kind: ServiceAccount
metadata:
  name: permission-service
  namespace: identity
  labels:
    app: permission-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-permission-service

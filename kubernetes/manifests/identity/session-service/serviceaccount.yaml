apiVersion: v1
kind: ServiceAccount
metadata:
  name: session-service
  namespace: identity
  labels:
    app: session-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-session-service

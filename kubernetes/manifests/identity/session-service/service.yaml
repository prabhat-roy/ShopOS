apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: identity
  labels:
    app: session-service
    domain: identity
spec:
  selector:
    app: session-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50062
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: mfa-service
  namespace: identity
  labels:
    app: mfa-service
    domain: identity
spec:
  selector:
    app: mfa-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50064
      targetPort: grpc
      protocol: TCP

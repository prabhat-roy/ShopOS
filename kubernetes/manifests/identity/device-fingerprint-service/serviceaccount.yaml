apiVersion: v1
kind: ServiceAccount
metadata:
  name: device-fingerprint-service
  namespace: identity
  labels:
    app: device-fingerprint-service
    domain: identity
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-device-fingerprint-service

apiVersion: v1
kind: ServiceAccount
metadata:
  name: revenue-recognition-service
  namespace: financial
  labels:
    app: revenue-recognition-service
    domain: financial
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-revenue-recognition-service

apiVersion: v1
kind: Service
metadata:
  name: chargeback-service
  namespace: financial
  labels:
    app: chargeback-service
    domain: financial
spec:
  selector:
    app: chargeback-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50191
      targetPort: grpc
      protocol: TCP

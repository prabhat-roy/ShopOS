apiVersion: v1
kind: ServiceAccount
metadata:
  name: expense-management-service
  namespace: financial
  labels:
    app: expense-management-service
    domain: financial
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-expense-management-service

apiVersion: v1
kind: Service
metadata:
  name: expense-management-service
  namespace: financial
  labels:
    app: expense-management-service
    domain: financial
spec:
  selector:
    app: expense-management-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50114
      targetPort: grpc
      protocol: TCP

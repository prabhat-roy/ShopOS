apiVersion: v1
kind: Service
metadata:
  name: budget-service
  namespace: financial
  labels:
    app: budget-service
    domain: financial
spec:
  selector:
    app: budget-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50117
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: ServiceAccount
metadata:
  name: invoice-service
  namespace: financial
  labels:
    app: invoice-service
    domain: financial
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-invoice-service

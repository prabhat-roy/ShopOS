apiVersion: apps/v1
kind: Deployment
metadata:
  name: tax-reporting-service
  namespace: financial
  labels:
    app: tax-reporting-service
    domain: financial
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tax-reporting-service
  template:
    metadata:
      labels:
        app: tax-reporting-service
        domain: financial
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "50113"
    spec:
      serviceAccountName: tax-reporting-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: tax-reporting-service
          image: shopos/tax-reporting-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50113
              protocol: TCP
          envFrom:
            - configMapRef:
                name: tax-reporting-service
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
          livenessProbe:
            tcpSocket:
              port: 50113
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50113
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: tax-reporting-service

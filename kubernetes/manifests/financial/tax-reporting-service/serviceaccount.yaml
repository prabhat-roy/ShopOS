apiVersion: v1
kind: ServiceAccount
metadata:
  name: tax-reporting-service
  namespace: financial
  labels:
    app: tax-reporting-service
    domain: financial
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-tax-reporting-service

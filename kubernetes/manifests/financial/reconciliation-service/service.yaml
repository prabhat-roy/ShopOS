apiVersion: v1
kind: Service
metadata:
  name: reconciliation-service
  namespace: financial
  labels:
    app: reconciliation-service
    domain: financial
spec:
  selector:
    app: reconciliation-service
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

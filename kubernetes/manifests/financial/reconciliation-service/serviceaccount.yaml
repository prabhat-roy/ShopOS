apiVersion: v1
kind: ServiceAccount
metadata:
  name: reconciliation-service
  namespace: financial
  labels:
    app: reconciliation-service
    domain: financial
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-reconciliation-service

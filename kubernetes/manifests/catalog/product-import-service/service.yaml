apiVersion: v1
kind: Service
metadata:
  name: product-import-service
  namespace: catalog
  labels:
    app: product-import-service
    domain: catalog
spec:
  selector:
    app: product-import-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50180
      targetPort: grpc
      protocol: TCP

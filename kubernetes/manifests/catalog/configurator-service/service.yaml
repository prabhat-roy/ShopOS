apiVersion: v1
kind: Service
metadata:
  name: configurator-service
  namespace: catalog
  labels:
    app: configurator-service
    domain: catalog
spec:
  selector:
    app: configurator-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50076
      targetPort: grpc
      protocol: TCP

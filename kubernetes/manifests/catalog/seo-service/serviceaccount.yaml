apiVersion: v1
kind: ServiceAccount
metadata:
  name: seo-service
  namespace: catalog
  labels:
    app: seo-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-seo-service

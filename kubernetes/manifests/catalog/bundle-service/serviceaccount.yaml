apiVersion: v1
kind: ServiceAccount
metadata:
  name: bundle-service
  namespace: catalog
  labels:
    app: bundle-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-bundle-service

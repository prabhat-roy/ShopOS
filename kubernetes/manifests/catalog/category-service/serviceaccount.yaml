apiVersion: v1
kind: ServiceAccount
metadata:
  name: category-service
  namespace: catalog
  labels:
    app: category-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-category-service

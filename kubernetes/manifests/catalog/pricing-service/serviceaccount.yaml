apiVersion: v1
kind: ServiceAccount
metadata:
  name: pricing-service
  namespace: catalog
  labels:
    app: pricing-service
    domain: catalog
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-pricing-service

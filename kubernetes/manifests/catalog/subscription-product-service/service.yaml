apiVersion: v1
kind: Service
metadata:
  name: subscription-product-service
  namespace: catalog
  labels:
    app: subscription-product-service
    domain: catalog
spec:
  selector:
    app: subscription-product-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50077
      targetPort: grpc
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: referral-service
  namespace: affiliate
  labels:
    app: referral-service
    domain: affiliate
spec:
  selector:
    app: referral-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50201
      targetPort: grpc
      protocol: TCP

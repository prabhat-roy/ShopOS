apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: commerce
  labels:
    app: fraud-detection-service
    domain: commerce
spec:
  selector:
    app: fraud-detection-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50091
      targetPort: grpc
      protocol: TCP

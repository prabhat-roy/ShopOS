apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: commerce
  labels:
    app: order-service
    domain: commerce
spec:
  selector:
    app: order-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50082
      targetPort: grpc
      protocol: TCP

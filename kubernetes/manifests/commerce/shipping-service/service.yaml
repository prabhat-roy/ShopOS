apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: commerce
  labels:
    app: shipping-service
    domain: commerce
spec:
  selector:
    app: shipping-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50084
      targetPort: grpc
      protocol: TCP

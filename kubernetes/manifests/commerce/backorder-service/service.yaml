apiVersion: v1
kind: Service
metadata:
  name: backorder-service
  namespace: commerce
  labels:
    app: backorder-service
    domain: commerce
spec:
  selector:
    app: backorder-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50099
      targetPort: grpc
      protocol: TCP

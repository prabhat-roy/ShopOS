apiVersion: v1
kind: ServiceAccount
metadata:
  name: wallet-service
  namespace: commerce
  labels:
    app: wallet-service
    domain: commerce
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-wallet-service

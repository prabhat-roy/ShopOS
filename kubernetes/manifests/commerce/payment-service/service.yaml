apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: commerce
  labels:
    app: payment-service
    domain: commerce
spec:
  selector:
    app: payment-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50083
      targetPort: grpc
      protocol: TCP

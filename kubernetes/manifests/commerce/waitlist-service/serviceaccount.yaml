apiVersion: v1
kind: ServiceAccount
metadata:
  name: waitlist-service
  namespace: commerce
  labels:
    app: waitlist-service
    domain: commerce
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-waitlist-service

apiVersion: v1
kind: ServiceAccount
metadata:
  name: voucher-service
  namespace: commerce
  labels:
    app: voucher-service
    domain: commerce
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::$(AWS_ACCOUNT_ID):role/shopos-voucher-service

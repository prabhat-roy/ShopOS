apiVersion: v1
kind: Service
metadata:
  name: digital-goods-service
  namespace: commerce
  labels:
    app: digital-goods-service
    domain: commerce
spec:
  selector:
    app: digital-goods-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50096
      targetPort: grpc
      protocol: TCP

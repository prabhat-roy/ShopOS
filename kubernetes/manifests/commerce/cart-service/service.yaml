apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: commerce
  labels:
    app: cart-service
    domain: commerce
spec:
  selector:
    app: cart-service
  type: ClusterIP
  ports:
    - name: grpc
      port: 50080
      targetPort: grpc
      protocol: TCP
